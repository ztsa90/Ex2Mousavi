server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tbl_Ex2Mousavi
    username: postgres
    password: sanaamiri

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
    show-sql: true